<h1>All Advertisements</h1>
<% @advertisements.each do |advertisement| %>
  <div class="media">
    <div class="media-body">
      <h4 class="media-headin">
        <%= link_to "#{advertisement.title} for $#{advertisement.price}", advertisement %> 
      </h4>    
    </div>
  </div>
<% end %>